{
  "name": "korrektur-dossier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/package-local.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "word": "node local/server.mjs --open"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.3.289",
    "pdfmake": "^0.2.20"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^6.4.1"
  }
}
